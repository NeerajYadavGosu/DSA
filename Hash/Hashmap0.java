
import java.util.*;
public class Hashmap0 {
    // read a paragraph and return the max rep
    //repeated element
    Scanner sc = new Scanner(System.in);
    int t = sc.nextInt();
    Map<String,Integer> map = new HashMap<>();
    while(t-- > 0){
        String str = sc.next();
        if(map.containsKey(str)){
            map.put(str , map.get(s));
        }
    }
    
}
